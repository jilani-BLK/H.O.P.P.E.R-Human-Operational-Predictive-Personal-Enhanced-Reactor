# Requirements for HOPPER Voice Improvement System
# Install with: pip install -r requirements-voice.txt

# Core dependencies
loguru>=0.7.0
pydub>=0.25.1

# Audio processing
noisereduce>=3.0.0
scipy>=1.11.0
numpy>=1.24.0
soundfile>=0.12.1

# TTS (install in separate venv recommended - Python 3.11 required)
# TTS>=0.22.0
# torch>=2.0.0
# torchaudio>=2.0.0

# Note: For TTS, use the dedicated venv_tts environment:
# ./venv_tts/bin/pip install TTS torch torchaudio
